import React from 'react';

import type { WeatherData } from '../types/weather';

interface CurrentWeatherProps {
  data: WeatherData;
}

export function CurrentWeather(_props: CurrentWeatherProps): React.JSX.Element {
  return <></>;
}
